# Network
listening-port=3478
min-port=${TURN_MIN_PORT}
max-port=${TURN_MAX_PORT}

# Identity
realm=${COTURN_DOMAIN}
server-name=${COTURN_DOMAIN}

# Auth — HMAC short-lived tokens, no static passwords
fingerprint
use-auth-secret
static-auth-secret=${TURN_SECRET}

# Only relay to valid WebRTC peers, block internal/private networks
no-multicast-peers
denied-peer-ip=0.0.0.0-0.255.255.255
denied-peer-ip=10.0.0.0-10.255.255.255
denied-peer-ip=100.64.0.0-100.127.255.255
denied-peer-ip=127.0.0.0-127.255.255.255
denied-peer-ip=169.254.0.0-169.254.255.255
denied-peer-ip=172.16.0.0-172.31.255.255
denied-peer-ip=192.0.0.0-192.0.0.255
denied-peer-ip=192.168.0.0-192.168.255.255
denied-peer-ip=198.18.0.0-198.19.255.255
denied-peer-ip=198.51.100.0-198.51.100.255
denied-peer-ip=203.0.113.0-203.0.113.255
denied-peer-ip=240.0.0.0-255.255.255.255

# Only allow UDP relay (what WebRTC actually uses)
no-tcp-relay

# Don't advertise software version
no-software-attribute

# Logging
log-file=stdout
no-stdout-log
simple-log
